# CLA signatures

This unprotected branch stores Individual CLA signatures written by
`contributor-assistant/github-action`.

Do not create `signatures/version1/cla.json` by hand. The action creates
that file on the first successful sign-off.

Do not add a branch protection rule that blocks `GITHUB_TOKEN` from
committing here.
